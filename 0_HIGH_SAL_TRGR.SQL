CREATE OR REPLACE TRIGGER HIGH_SAL BEFORE
  INSERT OR
  UPDATE ON EMP26 FOR EACH ROW WHEN (NEW.SALARY >=100000) DECLARE SAL NUMBER;
  BEGIN
    SAL    := :NEW.SALARY;
    IF SAL >= 100000 THEN
      RAISE_APPLICATION_ERROR(-20001,'SALARY IS MORE THAN 1 LAC');
    END IF;
  END;