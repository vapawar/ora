CREATE TABLE EMP26
(
ID NUMBER(8) PRIMARY KEY,
NAME VARCHAR2(20),
SALARY NUMBER(10,2)
);
INSERT INTO EMP26 VALUES(0000,'ABC',0000.00);

SHOW ERRORS;

EXECUTE EMP26_DATA;

SELECT * FROM EMP26 ORDER BY ID ASC;

EXECUTE EMP26_DUMP;

CREATE OR REPLACE DIRECTORY MYFILES AS '/USR/LIB/ORACLE/XE/DATA_DUMP/'

COMMIT;

SELECT * FROM ALL_DIRECTORIES;


SELECT LISTAGG(A, '') WITHIN GROUP( ORDER BY ROWNUM) FROM (WITH DATA AS  (SELECT SUBSTR(:VAR,LEVEL,1)||'*' A FROM DUAL CONNECT BY LEVEL <= LENGTH(:VAR))
SELECT * FROM DATA)
